# SkywalkerGameServer - Cursor Rules

## 项目概述

C++20游戏服务器框架，插件式架构，用于学习目的。

## 构建命令

```bash
# 构建项目
cd build/SkywalkerGameServer
cmake ..
cmake --build . --config Debug

# 运行服务器
./Bin/Debug/SkywalkerGameServer.exe
```

## 代码规范

### 核心原则

**所有代码必须写单元测试** - 没有单元测试的代码不应被提交。

### 架构原则

**使用插件-模块设计** - 所有功能都应以插件形式实现，每个插件可以包含多个模块。这样确保：
- 功能之间松耦合
- 易于启用/禁用功能
- 职责清晰分离

### 平台原则

**所有设计都必须跨平台** - 从一开始就为 Windows、Linux 和 iOS 设计。使用平台抽象层，避免在核心模块中直接使用平台特定代码。

### 文件组织
- `.h` 头文件，`.cpp` 实现文件
- 使用 `#ifndef __FILE_PATH_H__` 头保护
- 引入顺序：框架核心头 → 本地头

### 命名约定
- 类：`C` 前缀 (如 `CSkywalkerFramework`)
- 插件：`SSFPlugin_` 前缀
- 模块：`SSFModule_` 前缀
- 抽象接口：`I` 前缀
- 枚举：`E` 前缀
- 文件：PascalCase

### 类型系统 (使用框架类型)
```cpp
SSFString   // std::string
SSFBool     // bool
SSFInt      // int
SSFUInt     // unsigned int
SSFFloat    // float
SSFDouble   // double
```

### 指针宏
```cpp
SSF_PTR(T)           // 裸指针 T*
SSF_CONST_PTR(T)     // 常指针 const T*
SSF_SHARED_PTR(T)    // shared_ptr<T>
SSF_WEAK_PTR(T)      // weak_ptr<T>
SSF_UNIQUE_PTR(T)    // unique_ptr<T>
```

### 命名空间
```cpp
SSF_NAMESPACE_BEGIN
// 代码
SSF_NAMESPACE_END
```

### 错误处理
```cpp
SSFObjectErrors Errors;
SSF_ERROR(Errors, SkywalkerSFError_Plugin_Load_Failed);
SSF_ERROR_DESC(Errors, SkywalkerSFError_Plugin_Load_Failed, "描述");
if (Errors.IsValid()) { /* 处理错误 */ }
```

### 日志
```cpp
SSF_LOG_INFO("消息 " << 变量);
SSF_LOG_ERROR("消息 " << 变量);
SSF_LOG_DEBUG("消息 " << 变量);
```

### 断言
```cpp
SSF_ASSERT(条件);
SSF_ASSERT_IS_BASE_OF(SSFPlugin, MyPlugin);
```

### 插件开发
```cpp
// 注册插件
SSF_REGISTER_PLUGIN(PluginManager, SSFPlugin_MyPlugin);
// 注销插件
SSF_UNREGISTER_PLUGIN(PluginManager, SSFPlugin_MyPlugin);
// DLL导出
SSF_PLUGIN_EXPORT(SSFPlugin_MyPlugin);
```

### 模块开发
```cpp
SSF_REGISTER_MODULE(SSFModule_MyModule);
SSF_UNREGISTER_MODULE(SSFModule_MyModule);
```

### 代码组织
使用 `#pragma region` 划分代码区域：
```cpp
#pragma region SSFPlugin
// 相关代码
#pragma endregion SSFPlugin
```

### 文件头格式
```cpp
/*************************************************************************
**文件: SkywalkerFramework\Plugin\Test\SSFPlugin_Test.h
**作者: shyfan
**日期: 2023/08/21 19:54:39
**功能: 测试插件
*************************************************************************/
```

## 常见问题

1. **宏报错**: 检查是否使用了单行注释 → 改用多行注释
2. **枚举未声明**: 检查前一行是否有中文注释 → 尝试修改编码
3. **_CrtlsValidHeapPointer**: 错误释放了智能指针

## 目录结构

```
SkywalkerGameServer/
├── SkywalkerGameServer/      # 主服务器
├── SkywalkerGameClient/      # 测试客户端
├── SkywalkerServerEditor/   # 服务器编辑器
├── SkywalkerFramework/
│   ├── Core/                 # 核心库(静态)
│   ├── Plugin/               # 动态插件
│   │   ├── AI/ Actor/ CommandLine/
│   │   ├── DB/ Level/ Network/
│   │   ├── Profiler/ Test/ Video/
│   └── Include/              # 框架头文件
├── SkywalkerScript/         # 脚本工具
└── SkywalkerTools/          # 工具库
```

## 测试框架

使用 `SkywalkerTools/SkywalkerTest/SkywalkerTest.h` 单元测试框架。

### 编写测试

```cpp
#include "SkywalkerTest/SkywalkerTest.h"

// 定义测试套件
SKYWALKER_TEST_SUITE(MySuite)

// 编写测试函数
bool TestMyFeature()
{
    SKYWALKER_TEST_ASSERT_TRUE(someCondition);
    SKYWALKER_TEST_ASSERT_EQ(expected, actual);
    SKYWALKER_TEST_ASSERT_NE(expected, actual);
    return true;
}

// 注册测试（启动时自动注册）
SKYWALKER_TEST_REGISTER(MySuite, TestMyFeature, TestMyFeature)
```

### 运行测试

```cpp
#include "SkywalkerTest/SkywalkerTest.h"

int main()
{
    return SkywalkerTestRunner::Instance().RunAll();
}
```

### 测试宏

| 宏 | 描述 |
|---|---|
| `SKYWALKER_TEST_ASSERT(condition)` | 断言条件为真 |
| `SKYWALKER_TEST_ASSERT_EQ(expected, actual)` | 断言两值相等 |
| `SKYWALKER_TEST_ASSERT_NE(expected, actual)` | 断言两值不等 |
| `SKYWALKER_TEST_ASSERT_TRUE(value)` | 断言值为真 |
| `SKYWALKER_TEST_ASSERT_FALSE(value)` | 断言值为假 |

## 注意事项

- 注释使用中文
- 文档化所有public方法
- 测试通过 SkywalkerTest 框架自动化测试
